# 🤖 SmartVault Bot

**Умный бот для организации избранных сообщений**

---

## 🎯 Возможности

SmartVault помогает удобно управлять важными сообщениями с помощью системы тегов и интеллектуального поиска:

### ⭐ **Добавление в избранное**
- Сохраняйте важные сообщения с произвольными тегами
- Быстрый доступ к сохраненным материалам

### 🏷️ **Управление тегами**
- Создавайте персонализированную систему тегов
- Группируйте сообщения по категориям и темам
- Гибкая организация контента

### 👥 **Совместная работа**
- Создавайте общие избранные сообщения через групповые теги
- Идеально для командной работы и проектов

### 🔍 **Интеллектуальный поиск**
- Поиск сообщений по тегу или нескольким тегам

### 🖼️ **AI-распознавание**
- Автоматическое распознавание текста на изображениях с помощью искусственного интеллекта
- Сохранение текста из картинок с возможностью поиска

---

## 🚀 Установка и запуск бота

### Предварительные требования
- Docker
- Docker Compose
- Токен бота от [MaxAPI](https://maxapi.com/)

### Быстрый запуск

1. **Клонируйте репозиторий в любую папку:**
```bash
git clone <repository-url> <folder-name> 
cd <folder-name> 
```
2. **Добавьте файл с приватным токеном бота по пути ./code/bot/token.txt:**
```bash
echo <your-bot-token> > ./code/bot/token.txt
```

3. **Запустите бота.**
Для запуска предоставляются две конфигурации бота: 
- Основная конфигурация с использованием нейросети для распознавания символов на изображениях по команде пользователя (требуется GPU и подтягивание тяжелого образа)
```bash
docker-compose -f docker-compose-ocr-paddle.yml up -d
```
- Конфигурация с заглушкой вместо нейросети, подтягивающая минимальное количество зависимостей для быстрого запуска и проверки основного функционала
```bash
docker-compose -f docker-compose-ocr-stub.yml up -d
```

### 🗃️ Администрирование базы данных

Для удобного управления базой данных предусмотрен контейнер с **pgAdmin** - веб-интерфейсом для администрирования PostgreSQL.

**Доступ к pgAdmin:**
- **URL:** http://localhost:5050
- **Логин/пароль:** настраиваются в compose файлах, по умолчанию postgres postgres

Сервер для доступа к БД добавляется автоматически и доступен сразу после авторизации.
